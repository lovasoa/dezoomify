[package]
name = "dezoomify-job"
version = "1.0.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Deterministic portable job state machine for dezoomify-ng (phase 06 lean engine)."
repository = "https://github.com/lovasoa/dezoomify-ng"

[dependencies]
dezoomify-core = { path = "../dezoomify-core" }
dezoomify-protocol = { path = "../dezoomify-protocol" }
serde.workspace = true
serde_json.workspace = true
