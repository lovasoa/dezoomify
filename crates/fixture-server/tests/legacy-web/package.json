{
  "name": "legacy-web-harness",
  "version": "1.0.0",
  "private": true,
  "description": "Destination-side parity harness: untouched legacy web source against the new fixture server",
  "scripts": {
    "test": "playwright test && node recognition.js"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  },
  "license": "GPL-3.0-or-later"
}
