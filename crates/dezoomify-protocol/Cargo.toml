[package]
name = "dezoomify-protocol"
version = "1.0.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Canonical versioned protocol DTOs for dezoomify-ng hosts and shared logic."
repository = "https://github.com/lovasoa/dezoomify-ng"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
serde_json = "1"
