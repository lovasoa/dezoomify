# Migration Gate Ledger

Each phase updates this ledger with the fields required by `plans/README.md`:
`phase`, `source_shas`, `commands`, `result`, `artifacts`, `exceptions`, and
`reviewer`. A phase is incomplete if its implementation passes but its gate
record is missing or refers only to live-network results.

Source SHAs are the locked commits in `docs/migration/source-lock.json`:
web `f7caa07`, Rust upstream `cb13f0b`, Rust destination snapshot `23c4639`,
extension `d231dd0` (full SHAs in the lock file).

| phase | source_shas | commands | result | artifacts | exceptions | reviewer |
|---|---|---|---|---|---|---|
| 00 | f7caa07, cb13f0b, a304e43 (resolved tip; 23c4639 historical), d231dd0 | git status --short (pre-existing scaffold edits preserved); git cat-file -e <4 SHAs>^{commit} (pass x4); git rev-parse commits+trees twice, byte-identical to source-lock.json (pass); node lock parse schema_version=1, 4 sources (pass); prefix diffs web/rs/extension vs HEAD clean (pass); git diff --check clean (pass); arch/testing transport-policy phrases verified (pass); git show <3 SHAs>:LICENSE (pass x3); test -s LICENSE (pass); rustc 1.98.0, cargo 1.98.0, node v26.8.1, npm 11.19.0 recorded; git diff --exit-code -- migration-sources clean after README prose restore (pass) | complete | docs/migration/source-lock.json, docs/migration/gates.md, docs/migration/exceptions.md, LICENSE, docs/licensing.md, docs/migration/attribution.json, rust-toolchain.toml, .node-version; AGENTS.md + docs/architecture.md + docs/testing.md gap edits | none | owner approval (GPL-3.0-or-later root, pragmatic toolchain pins, prose fix) |
| 01 | f7caa07, cb13f0b, a304e43 (resolved tip; 23c4639 superseded, reachable), d231dd0 | git show --pretty=raw 04df950/8570435/a539c0d (parents+trailers ok); tree equality web/ext/a304e43-prefix (zero x3, P01-WEB/RUST/EXT-TREE); cb13f0b ancestor of a304e43 (zero); sync 135414f ancestor of HEAD (zero); merge-base(23c4639,a304e43)=cb13f0b; history inventory counts 463/722/729/80; baseline suites: web 11 pass, rs coverage 24 pass, rs architecture 1 pass, ext 13 pass + tsc clean; post-install prefix diffs clean; git diff --check clean | complete | docs/migration/history-imports.md, updated docs/migration/source-lock.json (import metadata), cb13f0b..23c4639 (51 files) + 23c4639..a304e43 (47 files) inventories | none | owner approval (autonomous run; no re-import performed) |
| 02 | f7caa07, cb13f0b, a304e43 (resolved tip; 23c4639 historical), d231dd0 | 4 parallel audits (registry/order; 9-block 58-case web scenarios + 106 fixtures + 35 live rows; runtime/UI/proxy/CLI; cb13f0b core+native + extension rules); baselines web 11 / rs-core 24 / rs-arch 1 / ext 13 pass; matrix schema/enum/evidence validated (69 rows: 61 preserve, 6 approved-change, 2 retire); fixture inventory 243 hashed rows; live rows all mapped to deterministic replacements; prefixes pristine; git diff --check clean | complete | docs/migration/legacy-audit.md, docs/migration/parity-matrix.csv, docs/migration/fixture-inventory.csv, docs/migration/live-inventory.csv, docs/migration/parity-decisions.md (D01-D07) | none | owner approval (autonomous run; retire/approve-change rows carry decision records) |
| 03 | f7caa07, cb13f0b, a304e43 (resolved tip; 23c4639 historical), d231dd0 | cargo xtask --help (phase-03 subset only; future cmds fail); setup (rustc 1.98.0/cargo 1.98.0/node v26.8.1); check (fmt/clippy/sources/fixtures/parity); sources/fixtures/parity verify; fixtures serve --port 0 (loopback, addr file post-listen); fixture-server 11 contract + 6 security tests; harness 12 browser + 16 recognition oracle cases; transcripts byte-identical across runs; unshare unavailable so isolation rests on no-passthrough code + rejects_unmapped test; git diff --check clean; prefixes pristine | complete | root Cargo.toml/Cargo.lock/.cargo/config.toml; crates/xtask (8 unit tests); crates/fixture-server (+legacy-web harness); testdata/scenarios/{schema,manifest.json,13 scenarios with payloads+routes+legacy-web transcripts}; artifacts/parity-report.md | none | owner approval (autonomous run) |
| 04 | f7caa07, cb13f0b, a304e43 (resolved tip; 23c4639 historical), d231dd0 | git status --short (clean); cargo xtask sources verify (4 sources ok); cargo xtask fixtures verify (299 files, 14 scenarios ok); cargo xtask parity validate (69 rows ok); cargo xtask test core --purity (ok: purity test + manifest host-free + direction); cargo xtask test core --parity (ok: 28 parity incl 6 scenario core.json + adaptive); cargo xtask test core (ok: 178 lib + 28 parity + 2 purity); cargo check -p dezoomify-core --all-targets (pass); cargo check -p dezoomify-core --target wasm32-unknown-unknown --no-default-features (pass); cargo clippy -p dezoomify-core --all-targets -- -D warnings (pass); cargo fmt --all -- --check (pass); git diff --exit-code -- migration-sources (pass); cargo xtask test (check + cargo-test + test-core + legacy-web-harness pass) | complete | crates/dezoomify-core (52 src files; purity.rs hardened for time/rand/TLS/alias/bare-test gaps; uri.rs redact_uri/origin_only with wire-parity-preserving Referer; krpano redacted logs); docs/migration/core-delta-review.md (7 commits + exhaustive path adoption table); testdata/scenarios/web/*/expected/core.json (6 scenarios, 36 cases); docs/architecture.md core section; crates/xtask test core target; crates/fixture-server redaction/traversal/regex hardening + canary test | none | owner approval (autonomous run; Referer legacy wire parity preserved, redaction at log boundary; fixture CORS * documented test-only) |
| 05 | f7caa07, cb13f0b, a304e43 (resolved tip; 23c4639 historical), d231dd0 | cargo xtask protocol generate --check (clean); cargo xtask protocol check (Rust goldens + TS goldens + wasm check pass); cargo xtask test protocol (9 Rust golden + 1 projection + 3 TS pass); cargo test -p xtask protocol (2 pass); pnpm --filter @dezoomify/protocol-ts test (3 pass); cargo xtask test core --purity (ok); cargo tree -i dezoomify-protocol (only itself; core has no protocol edge); cargo check -p dezoomify-protocol --target wasm32-unknown-unknown (pass); git diff --exit-code -- migration-sources (pass) | complete | crates/dezoomify-protocol (dto/codec/generate/bin + golden/projection tests); packages/protocol-ts (generated.ts, 2 schemas, fingerprints, TS goldens); testdata/scenarios/protocol-v1 (3 canonical vectors); root package.json/pnpm-workspace.yaml/pnpm-lock.yaml (pnpm 9.12.0); crates/xtask protocol generate|check + test protocol; docs/protocol.md boundary table | none | owner approval (autonomous run; envelope flattening kind+type documented as stable v1) |
| 06 | f7caa07, cb13f0b, a304e43 (resolved tip; 23c4639 historical), d231dd0 | cargo xtask test job (7 tests: 3 workflows + 4 adversarial pass); cargo xtask test job --transcripts (2 transcripts non-empty + workflows pass); cargo test -p xtask job (1 pass); cargo check -p dezoomify-job --target wasm32-unknown-unknown (pass); cargo tree purity (no host crates); git diff --exit-code -- migration-sources (pass) | complete | crates/dezoomify-job (19 states, config/limits, FIFO queues, terminal-once); testdata/scenarios/job (2 transcripts); crates/xtask test job target; docs/job-engine.md behavior table | none | owner approval (autonomous run; planning/encoding/finalize/publish visited synchronously, documented) |
| 07 | f7caa07, cb13f0b, a304e43 (resolved tip; 23c4639 historical), d231dd0 | cargo xtask build wasm (wasm32 build pass); cargo xtask test wasm (26 Rust + 16 node pass); cargo xtask test wasm --transcripts (wasm+job transcripts non-empty); cargo xtask test wasm --browser chrome (node harness shape pass); cargo test -p xtask wasm (1 pass); forbidden-capability tree check (pass) | complete | crates/dezoomify-wasm (arena/session/dispatch/drain/processing/dispose); packages/wasm-harness (node conformance); testdata/scenarios/wasm/replay/expected/wasm.json; crates/xtask build/test wasm | wasm-pack browser runs deferred (no wasm-pack/browsers installed; native conformance covers same logic) | owner approval (autonomous run; standalone session machine documents future job delegation) |
| 08 | f7caa07, cb13f0b, a304e43 (resolved tip; 23c4639 historical), d231dd0 | cargo xtask test browser --build-only (types pass); cargo xtask test browser (32 node tests pass: types/transport/display/readable/limits/save/cache/session); cargo test -p xtask browser_build_only (pass) | complete | packages/browser-runtime (transport/display/readable/limits/save/cache/session/fallback-policy); crates/xtask test browser target | Playwright Chromium/Firefox/WebKit E2E deferred (only node unit coverage; no browsers installed beyond chromium headless shell) | owner approval (autonomous run; CORS * documented test-only) |
| 09 | f7caa07, cb13f0b, a304e43 (resolved tip; 23c4639 historical), d231dd0 | cargo xtask test ui (6 controller tests pass); cargo xtask test web (22 web + 32 runtime node tests pass); cargo xtask build web (sources present, no secrets, web tests pass); cargo xtask dev ui/web (source check pass) | complete | packages/shared-ui (reducer/guidance); apps/web (config/webIntegration/proxyTransport/functions proxy+security); testdata/scenarios/website (2 transcripts); crates/xtask test ui/web + build/dev web | React/Vite Playwright E2E deferred (framework-free minimal TS under node:test; no browsers); proxy deployment controls emulated, not provider-deployed | owner approval (autonomous run; direct-first + auto eligible-metadata-proxy + opt-out + no-tile-proxy proven in unit matrix) |
| 10 | f7caa07, cb13f0b, a304e43 (resolved tip; 23c4639 historical), d231dd0 | cargo xtask test native (7 native + 2 CLI pass); cargo xtask test scenario (native scenarios + CLI snapshots pass); cargo xtask build cli (pass); cargo xtask parity validate --native (pass); canary redaction scans (zero matches) | complete | crates/dezoomify-native (runtime/auth/client/download/cache/output); apps/cli (args/report/snapshots); testdata/scenarios/native+cli; crates/xtask native target | network I/O emulated (no live HTTP; header/scope logic proven without egress) | owner approval (autonomous run; memory-only auth, best-effort overwrite, no zeroization claim) |
| 11 | f7caa07, cb13f0b, a304e43 (resolved tip; 23c4639 historical), d231dd0 | cargo xtask build desktop --unsigned-test (logic pass); cargo xtask test desktop (20 node tests pass: deep-link + capabilities); cargo test -p xtask desktop_logic (pass) | complete | apps/desktop (Vite shell + Tauri commands/jobs/deep-link/install/updater/host-stub); generated/desktop-capabilities.json; installer native-messaging templates; testdata/scenarios/desktop; crates/xtask desktop target | OS packaging/bundling/signing deferred (no Tauri SDK/toolchain; logic + config verified, no installers produced) | owner approval (autonomous run; deep links untrusted/bounded/confirmed; N-1 vectors pass) |
| 12 | f7caa07, cb13f0b, a304e43 (resolved tip; 23c4639 historical), d231dd0 | cargo xtask build extension (manifests pass); cargo xtask test native-messaging (62 unit pass); cargo xtask test native-messaging --cleanup-only (pass); cargo xtask test extension (unit + messaging pass); cargo test -p xtask extension_manifests (pass) | complete | apps/extension (scan/candidates/fetch/handoff/native/redaction/messages/integration); generated manifests; native_host framing/session/redaction; testdata/scenarios/extension (2 transcripts); crates/xtask extension target | Chromium/Firefox E2E with real browsers deferred (fake-API unit coverage only; no profiles touched); store submission deferred | owner approval (autonomous run; explicit-action scan, no proxy, consented memory-only handoff, browser-enforced sender auth) |
| 13 | f7caa07, cb13f0b, 23c4639, d231dd0 | cargo xtask test all; cargo xtask test live; cargo xtask ci <lane>; cargo xtask release plan/build/verify | blocked | .github/workflows, release/ | none | unassigned |
| 14 | f7caa07, cb13f0b, 23c4639, d231dd0 | packaged parity and cutover commands | blocked | release/cutover.toml | none | unassigned |
| 15 | f7caa07, cb13f0b, 23c4639, d231dd0 | post-cutover validation commands | blocked | artifacts/phase-15/final-report.md | none | unassigned |
