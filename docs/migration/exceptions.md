# Migration Exceptions

An exception record must contain a unique ID, affected parity IDs, owner,
rationale, user impact, compensating test, expiry phase/date, and approval.
Empty exception fields are not an approval.

| id | affected_parity_ids | owner | rationale | user_impact | compensating_test | expiry | approval |
|---|---|---|---|---|---|---|---|
| E01 | P07-PACKAGE | owner | wasm-pack browser runs need pinned wasm-pack (absent); full Chromium 149 engine is present and `cargo build -p dezoomify-wasm --target wasm32-unknown-unknown` produces `dezoomify_wasm.wasm`; native conformance covers the same adapter logic | no wasm-pack packaging proof in this environment | `cargo test -p dezoomify-wasm` (26) + `packages/wasm-harness` node suite (16) + `cargo xtask build wasm` artifact exists | phase 13 rehearsal with pinned wasm-pack | owner approval (autonomous run) |
| E02 | P08-E2E, P09-E2E, P12-E2E | owner | Chromium E2E proven via legacy-web harness on full Chromium 149 (12 browser + 16 recognition); Firefox/WebKit engines declined by owner (Chromium-only accepted gap) + React/Vite wiring unproven | Firefox/WebKit E2E covered by node unit matrix only, per owner decision | legacy-web harness (real Chromium) + `cargo xtask test browser/ui/web/extension` node suites (32+6+22+62) | closed as accepted gap (owner decision 2026-09-05); reopen with `npx playwright install firefox webkit --with-deps` if needed | owner approval (Chromium-only, autonomous run) |
| E03 | P10-EGRESS | owner | native/CLI runtime HTTP is emulated (header/scope/cache logic without bundled egress); real-byte proof lives in `cargo xtask test live --public` (L22 krpano XML+89KB JPEG, L33 NLS IIIF JSON+17KB JPEG, L32 OSD DZI+21KB JPEG, verified 2026-09-05) | no in-tree runtime egress path; live proof is explicit and opt-in | `cargo xtask test live --public` (3 sites, magic-byte verified) + native scenario + redaction + CLI snapshot tests | phase-14 packaged parity on native OS runners | owner approval (autonomous run) |
| E04 | P11-PACKAGE | owner | OS packaging needs Tauri SDK + platform toolchains (absent); paid signing (Apple Developer ID/notarization, Azure Trusted Signing) removed from plan 2026-09-05 — recurring fees incompatible with a free project; installers ship unsigned, updater uses free self-generated keys | no installers produced; no paid signing ever | desktop deep-link + capability node tests + config validation | native-OS release lane (unsigned) | owner approval (autonomous run) |
| E05 | P12-STORE, P13-SIGN, P14-CUTOVER, P15-OBSERVE | owner | store-submit CI packages + draft-uploads to the EXISTING Chromium listing `iapjjopjejpelnfdonefbffahmcndfbm` (release/config.toml; never a new store item; CHROME_WS_CLIENT_ID/SECRET/REFRESH_TOKEN set 2026-09-05; no EXTENSION_ID secret needed — the public ID lives in release config; draft v1.1.0 uploadState SUCCESS 2026-09-05; draft v2.0 uploadState SUCCESS 2026-09-05; API publish refused with `Publish condition not met` — the listing's Privacy practices tab must be completed in the dev console before any publish); signing keys, production deploys, and prod observation need protected credentials/environments | test-channel candidate only; no production effects; store draft upload enabled via workflow_dispatch (upload=true), publish stays manual | `store-submit` dry-run (package + validate + artifact) + `cargo xtask release plan/build/verify` (test channel) + cutover/postcutover lanes on fixtures state | production cutover with approvals | owner approval (autonomous run) |
| E06 | P03-TRANSCRIPT | owner | legacy-web oracle transcripts are order-racy (concurrent tile insertion); harness defaults to compare-only, rewrite only with UPDATE_TRANSCRIPTS=1 | tree stays clean; drift fails loudly | `writeTranscript` compare gate + ordered lastTile + `git status --porcelain -- testdata/scenarios` | harness stabilization in a browser-equipped runner | owner approval (autonomous run) |
