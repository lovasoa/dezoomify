# Migration Exceptions

An exception record must contain a unique ID, affected parity IDs, owner,
rationale, user impact, compensating test, expiry phase/date, and approval.
Empty exception fields are not an approval.

| id | affected_parity_ids | owner | rationale | user_impact | compensating_test | expiry | approval |
|---|---|---|---|---|---|---|---|
| E01 | P07-PACKAGE | closed 2026-09-05 (C4) | The wasm adapter now delegates its whole lifecycle to `dezoomify-job` (`cargo xtask test wasm --transcripts` asserts engine-driven ids and progress/catalog events); wasm-bindgen glue is generated by `cargo xtask build web` into `wasm/` and the webapp E2E loads it in Chromium. wasm-pack (0.15) is now installed and the conformance suite runs on the wasm target through it: `wasm-pack test --node` (wired into the wasm-harness gate) and real-Chromium runs via `crates/dezoomify-wasm/scripts/browser-test.sh` (matching chromedriver auto-fetched) | wasm-pack packaging proof still outstanding; the engine gap this exception recorded is gone | `cargo test -p dezoomify-wasm` + `packages/wasm-harness` node suite + `cargo xtask build web` + webapp E2E (real Chromium, real save) | — | owner approval (autonomous run) |
| E02 | P08-E2E, P09-E2E, P12-E2E | owner (narrowed 2026-09-05, C5) | Chromium E2E now covers BOTH legacy-web harness (12 browser + 16 recognition) and the new webapp (`webapp-e2e`: discovery via worker-hosted wasm core, tile download, canvas assembly, real save with decoded-pixel verification, plus an honest no-signal failure test). Firefox/WebKit engines declined by owner (Chromium-only accepted gap) | Firefox/WebKit E2E covered by node unit matrix only, per owner decision | legacy-web harness + webapp-e2e (real Chromium) + `cargo xtask test browser/ui/web` node suites | closed as accepted gap (owner decision 2026-09-05); reopen with `npx playwright install firefox webkit --with-deps` if needed | owner approval (Chromium-only, autonomous run) |
| E03 | P10-EGRESS | closed 2026-09-05 (C1+C3) | Real rustls egress lives in `dezoomify-native` (`http.rs`): manual redirects with per-URL header rebuild, size/time limits, bounded retries, origin-scoped credentials; loopback socket tests cover redirects/limits/retries/failures. `cargo xtask test live --public` drives the real CLI against all 31 still-alive legacy rs targets (31 passed, 3 http-only policy rows documented, verified 2026-09-05) | — | `cargo xtask test native` + `cargo xtask test scenario` + `cargo xtask test live --public` | — | owner approval (autonomous run) |
| E04 | P11-PACKAGE | owner | OS packaging needs Tauri SDK + platform toolchains (absent); paid signing (Apple Developer ID/notarization, Azure Trusted Signing) removed from plan 2026-09-05 — recurring fees incompatible with a free project; installers ship unsigned, updater uses free self-generated keys | no installers produced; no paid signing ever | desktop deep-link + capability node tests + config validation | native-OS release lane (unsigned) | owner approval (autonomous run) |
| E05 | P12-STORE, P13-SIGN, P14-CUTOVER, P15-OBSERVE | owner | store-submit CI packages + draft-uploads to the EXISTING Chromium listing `iapjjopjejpelnfdonefbffahmcndfbm` (release/config.toml; never a new store item; CHROME_WS_CLIENT_ID/SECRET/REFRESH_TOKEN set 2026-09-05; no EXTENSION_ID secret needed — the public ID lives in release config; draft v1.1.0 uploadState SUCCESS 2026-09-05; draft v2.0 uploadState SUCCESS 2026-09-05; API publish `status ["OK"]` 2026-09-05 (store review pending)); signing keys, production deploys, and prod observation need protected credentials/environments | test-channel candidate only; no production effects; store draft upload enabled via workflow_dispatch (upload=true), publish stays manual | `store-submit` dry-run (package + validate + artifact) + `cargo xtask release plan/build/verify` (test channel) + cutover/postcutover lanes on fixtures state | production cutover with approvals | owner approval (autonomous run) |
| E06 | P03-TRANSCRIPT | owner | legacy-web oracle transcripts are order-racy (concurrent tile insertion); harness defaults to compare-only, rewrite only with UPDATE_TRANSCRIPTS=1 | tree stays clean; drift fails loudly | `writeTranscript` compare gate + ordered lastTile + `git status --porcelain -- testdata/scenarios` | harness stabilization in a browser-equipped runner | owner approval (autonomous run) |
