// Desktop entry point (phase-11 lean shell).
//
// Tauri `generate_context!` wiring is deferred until the Tauri SDK is
// vendored. The checked-in tauri.conf.json and capabilities describe the
// single local window, strict navigation policy, and minimal command surface.
// This stub keeps the standalone manifest checkable offline.

fn main() {
    println!("dezoomify-desktop 0.1.0 (phase-11 shell)");
}
