{
  "_compatNote": "Firefox runs background/index.js as an MV3 event page (non-persistent scripts entry; modules unsupported, so the entry ships as a classic script). Firefox and Chromium use the same finite scripting.executeScript source operations; no registered collector, reload, or persistent source-tab listener is used. No offscreen document: offscreen is Chromium-only and unnecessary for source operations.",
  "background": {
    "scripts": ["background/index.js"]
  },
  "browser_specific_settings": {
    "gecko": {
      "id": "{14074c89-8a5f-4813-98df-a7117f062871}",
      "strict_min_version": "128.0"
    }
  }
}
