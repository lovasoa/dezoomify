// Website compatibility entry point. The implementation lives in the shared
// browser runtime so production and tests compile and execute the same policy.
export * from "../packages/browser-runtime/src/web-integration.ts";
