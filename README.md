# Dezoomify

This repository unifies the Dezoomify website, browser extension, native
application, and command-line application around one Rust core and one shared
user interface.

The repository is being assembled from the existing projects. See
[`plans/`](plans/) for the migration procedure and [`docs/`](docs/) for the
intended final architecture.
