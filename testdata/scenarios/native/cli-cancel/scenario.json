{
  "description": "CLI/native pipeline stops at the next effect boundary when cancelled, reports job.cancelled, and writes no output",
  "id": "native/cli-cancel",
  "input": {
    "mode": "gateway",
    "url": "http://{{origin}}/fetch?url=https://fixtures.test/cli/pyramid.dzi"
  },
  "operation": "download-failure",
  "source_evidence": {
    "path": "crates/dezoomify-native/tests/pipeline_loopback.rs",
    "snapshot": "local"
  }
}
