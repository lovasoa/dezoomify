name: release-publish
on:
  workflow_dispatch:
    inputs:
      artifacts:
        required: true
        type: string
permissions:
  contents: read
jobs:
  publish:
    runs-on: ubuntu-latest
    environment: release-publish
    timeout-minutes: 60
    steps:
      # Honest stub: publishing is not wired; fail closed instead of
      # green-checking an echo as if artifacts were published.
      - run: echo "STUB: release publishing not implemented; no artifacts published" && exit 1
