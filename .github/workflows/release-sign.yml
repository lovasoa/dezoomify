name: release-sign
on:
  workflow_dispatch:
    inputs:
      run_id:
        required: true
        type: string
permissions: {}
jobs:
  sign:
    runs-on: ubuntu-latest
    environment: release-signing
    timeout-minutes: 60
    steps:
      # Honest stub: signing is not wired; fail closed instead of green-checking
      # an echo as if artifacts were signed.
      - run: echo "STUB: release signing not implemented; no artifacts signed" && exit 1
