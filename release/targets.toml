[[target]]
name = "cli-linux-x86_64"
os = "linux"

# Desktop release stays unavailable until a release build recipe lands;
# local `cargo xtask build desktop` already produces a real unsigned .deb
# (no paid signing) from the Tauri window shell behind the `tauri` feature.
[[target]]
name = "desktop-windows-x86_64"
os = "windows"
available = false

[[target]]
name = "extension-chromium"
os = "any"

[[target]]
name = "extension-firefox"
os = "any"
